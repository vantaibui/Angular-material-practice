import { ActiveEffectDirective } from './active-effect.directive';

describe('ActiveEffectDirective', () => {
  it('should create an instance', () => {
    const directive = new ActiveEffectDirective();
    expect(directive).toBeTruthy();
  });
});
